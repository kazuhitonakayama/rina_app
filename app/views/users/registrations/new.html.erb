<div class="signup-container">
  <div class="login-upper-area-container">
    <%= image_tag "signup_banner.svg" %>
  </div>

<div class="signupform-area">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "users/shared/error_messages", resource: resource %>

  <div class="icon">
    <%= f.file_field :image %>
  </div>

  <div class="form">
    <%= f.text_field :name,placeholder: "  ユーザー名",class: 'formbox' %>
  </div>
  <div class="form">
    <%= f.email_field :email, autofocus: true, autocomplete: "email",placeholder: "  メールアドレス",class: 'formbox' %>
  </div>
  <div class="form">
    <% if @minimum_password_length %>
    <% end %>
    <%= f.password_field :password, autocomplete: "new-password",placeholder: "  パスワード(6文字以上）",class: 'formbox'  %>
  </div>
  <div class="form">
    <%= f.password_field :password_confirmation, autocomplete: "new-password",placeholder: "  パスワード確認",class: 'formbox' %>
  </div>

  <div class="registeractions">
    <%= f.submit "登録",class: 'register_btn' %>
  </div>
</div>
<% end %>

<%= render "users/shared/links" %>

<%= image_tag "rina_loginwave.svg",class: "signup-bottom-wave" %>
</div>